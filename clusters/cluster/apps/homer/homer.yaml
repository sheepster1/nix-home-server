apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: homer
  namespace: apps
spec:
  releaseName: homer
  interval: 1m
  install:
    timeout: 20m
  upgrade:
    timeout: 20m
  chart:
    spec:
      chart: homer
      version: 8.1.15
      sourceRef:
        kind: HelmRepository
        name: djjudas21
        namespace: apps
  values:
    env:
      TZ: Asia/Jerusalem
    configmap:
      config:
        enabled: true
        data:
          config.yml: |
            # Homepage configuration
            # See https://fontawesome.com/search for icons options

            title: "Dashboard"
            subtitle: "dashboard"
            logo: "logo.png"
            # icon: "fas fa-skull-crossbones" # Optional icon

            header: true
            footer: '<p>Created with <span class="has-text-danger">❤️</span> with <a href="https://bulma.io/">bulma</a>, <a href="https://vuejs.org/">vuejs</a> & <a href="https://fontawesome.com/">font awesome</a> // Fork me on <a href="https://github.com/bastienwirtz/homer"><i class="fab fa-github-alt"></i></a></p>' # set false if you want to hide it.

            # Optional theme customization
            theme: default

            # Optional theme customization (color overrrides)
            # overrrides can also be done using CSS vars 
            colors:
              light:
                highlight-primary: "#3367d6"
                highlight-secondary: "#4285f4"
                highlight-hover: "#5a95f5"
                background: "#f5f5f5"
                card-background: "#ffffff"
                text: "#363636"
                text-header: "#ffffff"
                text-title: "#303030"
                text-subtitle: "#424242"
                card-shadow: rgba(0, 0, 0, 0.1)
                link: "#3273dc"
                link-hover: "#363636"
              dark:
                highlight-primary: "#3367d6"
                highlight-secondary: "#4285f4"
                highlight-hover: "#5a95f5"
                background: "#131313"
                card-background: "#2b2b2b"
                text: "#eaeaea"
                text-header: "#ffffff"
                text-title: "#fafafa"
                text-subtitle: "#f5f5f5"
                card-shadow: rgba(0, 0, 0, 0.4)
                link: "#3273dc"
                link-hover: "#ffdd57"

            # Optional message
            # message:
            #   #url: https://b4bz.io
            #   style: "is-dark" # See https://bulma.io/documentation/components/message/#colors for styling options.
            #   title: "Demo !"
            #   icon: "fa fa-grin"
            #   content: "This is a dummy homepage demo. <br /> Find more information on <a href='https://github.com/bastienwirtz/homer'>github.com/bastienwirtz/homer</a>"

            # Optional navbar
            # links: [] # Allows for navbar (dark mode, layout, and search) without any links
            links:
              # - name: "Contribute"
              #   icon: "fab fa-github"
              #   url: "https://github.com/bastienwirtz/homer"
              #   target: "_blank" # optional html a tag target attribute
              # - name: "Wiki"
              #   icon: "fas fa-book"
              #   url: "https://www.wikipedia.org/"
              # this will link to a second homer page that will load config from additional-page.yml and keep default config values as in config.yml file
              # see url field and assets/additional-page.yml.dist used in this example:
              #- name: "another page!"
              #  icon: "fas fa-file-alt"
              #  url: "#additional-page" 

            # Services
            # First level array represent a group.
            # Leave only a "items" key if not using group (group name, icon & tagstyle are optional, section separation will not be displayed).
            services:
              - name: "Media"
                items:
                  - name: "Jellyfin"
                    subtitle: "Media server"
                    url: "http://jellyfin.${cluster_domain}"
                  - name: "Jellyseerr"
                    subtitle: "Media server"
                    url: "http://jellyseerr.${cluster_domain}"
                  - name: "Bazarr"
                    subtitle: "Media server"
                    url: "http://bazarr.${cluster_domain}"
                  - name: "Flaresolverr"
                    subtitle: "Media server"
                    url: "http://flaresolverr.${cluster_domain}"
                  - name: "Jellyfin"
                    subtitle: "Media server"
                    url: "http://jellyfin.${cluster_domain}"
                  - name: "Prowlarr"
                    subtitle: "Media server"
                    url: "http://prowlarr.${cluster_domain}"
                  - name: "Qbittorrent"
                    subtitle: "Media server"
                    url: "http://qbittorrent.${cluster_domain}"
                  - name: "Radarr"
                    subtitle: "Media server"
                    url: "http://radarr.${cluster_domain}"
                  - name: "Sonarr"
                    subtitle: "Media server"
                    url: "http://sonarr.${cluster_domain}"

              - name: "General"
                items:
                  - name: "FileBrowser"
                    subtitle: "File browser"
                    url: "http://filebrowser.${cluster_domain}"
                  - name: "HomeAssistant"
                    subtitle: "HA"
                    url: "http://homeassistant.${cluster_domain}"
                  - name: "zwave-js-ui"
                    subtitle: "ZwaveJs UI"
                    url: "http://zwavejs.${cluster_domain}"
